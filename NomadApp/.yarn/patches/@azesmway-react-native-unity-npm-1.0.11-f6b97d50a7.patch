diff --git a/ios/RNUnityView.h b/ios/RNUnityView.h
index 23824e4..43c471d 100644
--- a/ios/RNUnityView.h
+++ b/ios/RNUnityView.h
@@ -1,7 +1,7 @@
 #import <React/RCTView.h>
 #import <React/RCTEventDispatcher.h>
 #include <UnityFramework/UnityFramework.h>
-#include <UnityFramework/NativeCallProxy.h>
+#import "NativeCallProxy.h"
 
 // This guard prevent this file to be compiled in the old architecture.
 #ifdef RCT_NEW_ARCH_ENABLED
diff --git a/react-native-unity.podspec b/react-native-unity.podspec
index af0ea2c..6df4945 100644
--- a/react-native-unity.podspec
+++ b/react-native-unity.podspec
@@ -14,7 +14,7 @@ Pod::Spec.new do |s|
   s.platforms    = { :ios => "12.4" }
   s.source       = { :git => "https://github.com/azesmway/react-native-unity.git", :tag => "#{s.version}" }
 
-  s.source_files = "ios/**/*.{h,m,mm}"
+  s.source_files = ["ios/**/*.{h,m,mm}", "unity/Assets/Plugins/iOS/**/*.{h,m,mm}"]
 
   # Use install_modules_dependencies helper to install the dependencies if React Native version >=0.71.0.
   # See https://github.com/facebook/react-native/blob/febf6b7f33fdb4904669f99d795eba4c0f95d7bf/scripts/cocoapods/new_architecture.rb#L79.
diff --git a/ios/RNUnityView.mm b/ios/RNUnityView.mm
index a4caa74..2c12ba6 100644
--- a/ios/RNUnityView.mm
+++ b/ios/RNUnityView.mm
@@ -9,6 +9,12 @@
 NSString *bundlePathStr = @"/Frameworks/UnityFramework.framework";
 int gArgc = 1;
 
+@interface RNUnityView ()
+
+@property (nonatomic, strong) NSMutableArray<NSDictionary *> *pendingUnityMessages;
+
+@end
+
 UnityFramework* UnityFrameworkLoad() {
     NSString* bundlePath = nil;
     bundlePath = [[NSBundle mainBundle] bundlePath];
@@ -42,9 +48,35 @@ - (bool)unityIsInitialized {
     return [self ufw] && [[self ufw] appController];
 }
 
+- (void)attachUnityRootViewIfNeeded {
+    if (![self unityIsInitialized]) {
+        return;
+    }
+
+    UIView *rootView = self.ufw.appController.rootView;
+    if (rootView == nil) {
+        return;
+    }
+
+    self.backgroundColor = UIColor.clearColor;
+    self.clipsToBounds = YES;
+    rootView.frame = self.bounds;
+    rootView.autoresizingMask = UIViewAutoresizingFlexibleWidth | UIViewAutoresizingFlexibleHeight;
+    rootView.contentScaleFactor = UIScreen.mainScreen.scale;
+
+    if (rootView.superview != self) {
+        [rootView removeFromSuperview];
+        [self insertSubview:rootView atIndex:0];
+    } else {
+        [self sendSubviewToBack:rootView];
+    }
+}
+
 - (void)initUnityModule {
     @try {
         if([self unityIsInitialized]) {
+            [self attachUnityRootViewIfNeeded];
+            [self flushPendingMessages];
             return;
         }
 
@@ -73,8 +105,11 @@ - (void)initUnityModule {
         [[[[self ufw] appController] window] addSubview: self.ufw.appController.rootView];
         [[[[self ufw] appController] window] makeKeyAndVisible];
         [[[[[[self ufw] appController] window] rootViewController] view] setNeedsLayout];
+        [self attachUnityRootViewIfNeeded];
 
         [NSClassFromString(@"FrameworkLibAPI") registerAPIforNativeCalls:self];
+        [self flushPendingMessages];
+        [self sendMessageToMobileApp:@"{\"status\":\"native_unity_initialized\"}"];
     }
     @catch (NSException *e) {
         NSLog(@"%@",e);
@@ -85,8 +120,7 @@ - (void)layoutSubviews {
    [super layoutSubviews];
 
    if([self unityIsInitialized]) {
-      self.ufw.appController.rootView.frame = self.bounds;
-      [self addSubview:self.ufw.appController.rootView];
+      [self attachUnityRootViewIfNeeded];
    }
 }
 
@@ -108,13 +142,21 @@ - (void)unloadUnity {
 }
 
 - (void)sendMessageToMobileApp:(NSString *)message {
-    if (self.onUnityMessage) {
+    if (!message || !self.onUnityMessage) {
+        return;
+    }
+
+    dispatch_async(dispatch_get_main_queue(), ^{
+        if (!self.onUnityMessage) {
+            return;
+        }
+
         NSDictionary* data = @{
             @"message": message
         };
 
         self.onUnityMessage(data);
-    }
+    });
 }
 
 - (void)unityDidUnload:(NSNotification*)notification {
@@ -149,10 +191,46 @@ - (dispatch_queue_t)methodQueue {
 
 - (void)postMessage:(NSString *)gameObject methodName:(NSString*)methodName message:(NSString*) message {
     dispatch_async(dispatch_get_main_queue(), ^{
+        if (![self unityIsInitialized]) {
+            [self initUnityModule];
+        }
+
+        if (![self unityIsInitialized]) {
+            if (self.pendingUnityMessages == nil) {
+                self.pendingUnityMessages = [NSMutableArray new];
+            }
+
+            [self.pendingUnityMessages addObject:@{
+                @"gameObject": gameObject ?: @"",
+                @"methodName": methodName ?: @"",
+                @"message": message ?: @""
+            }];
+            return;
+        }
+
         [[self ufw] sendMessageToGOWithName:[gameObject UTF8String] functionName:[methodName UTF8String] message:[message UTF8String]];
     });
 }
 
+- (void)flushPendingMessages {
+    if (![self unityIsInitialized] || self.pendingUnityMessages.count == 0) {
+        return;
+    }
+
+    NSArray<NSDictionary *> *queuedMessages = [self.pendingUnityMessages copy];
+    [self.pendingUnityMessages removeAllObjects];
+
+    for (NSDictionary *payload in queuedMessages) {
+        NSString *gameObject = payload[@"gameObject"] ?: @"";
+        NSString *methodName = payload[@"methodName"] ?: @"";
+        NSString *message = payload[@"message"] ?: @"";
+
+        [[self ufw] sendMessageToGOWithName:[gameObject UTF8String]
+                               functionName:[methodName UTF8String]
+                                    message:[message UTF8String]];
+    }
+}
+
 #ifdef RCT_NEW_ARCH_ENABLED
 - (void)prepareForRecycle {
     [super prepareForRecycle];
@@ -177,6 +255,7 @@ - (instancetype)initWithFrame:(CGRect)frame {
   if (self = [super initWithFrame:frame]) {
     static const auto defaultProps = std::make_shared<const RNUnityViewProps>();
     _props = defaultProps;
+    self.pendingUnityMessages = [NSMutableArray new];
 
     self.onUnityMessage = [self](NSDictionary* data) {
       if (_eventEmitter != nil) {
@@ -218,6 +297,7 @@ -(id)initWithFrame:(CGRect)frame {
     self = [super initWithFrame:frame];
 
     if (self) {
+        self.pendingUnityMessages = [NSMutableArray new];
         [self initUnityModule];
     }
 
diff --git a/ios/RNUnityViewManager.mm b/ios/RNUnityViewManager.mm
index f9b79bd..6e917a8 100644
--- a/ios/RNUnityViewManager.mm
+++ b/ios/RNUnityViewManager.mm
@@ -41,7 +41,7 @@ + (BOOL)requiresMainQueueSetup {
             RCTLogError(@"Cannot find NativeView with tag #%@", reactTag);
             return;
         }
-        [unity postMessage:(NSString *)gameObject methodName:(NSString *)methodName message:(NSString *)message];
+        [view postMessage:(NSString *)gameObject methodName:(NSString *)methodName message:(NSString *)message];
     }];
 }
 
@@ -52,7 +52,7 @@ + (BOOL)requiresMainQueueSetup {
            RCTLogError(@"Cannot find NativeView with tag #%@", reactTag);
            return;
        }
-       [unity pauseUnity:(BOOL * _Nonnull)pause];
+       [view pauseUnity:(BOOL * _Nonnull)pause];
    }];
 }
 
@@ -63,7 +63,7 @@ + (BOOL)requiresMainQueueSetup {
            RCTLogError(@"Cannot find NativeView with tag #%@", reactTag);
            return;
        }
-       [unity pauseUnity:(BOOL * _Nonnull)false];
+       [view pauseUnity:(BOOL * _Nonnull)false];
    }];
 }
 
@@ -74,7 +74,7 @@ + (BOOL)requiresMainQueueSetup {
            RCTLogError(@"Cannot find NativeView with tag #%@", reactTag);
            return;
        }
-       [unity unloadUnity];
+       [view unloadUnity];
    }];
 }
 
